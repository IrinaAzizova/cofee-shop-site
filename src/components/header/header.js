import './header.scss';

import NavLinks from '../nav-links/nav-links';

const Header = () => {
    return (
        <header>
            <nav>
                <NavLinks/>
            </nav>
        </header>
    )
};

export default Header;